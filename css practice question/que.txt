Recreate the following element using given css
. Create a div. & 3 sibling divs inside it
. For the outer div set following styling
   height 200px,width 70px, and boarder as 2px solid black color

.For the inner divs set the following styling  
    height 50px, width 50px, and boarder as 2px, solid black color
    also set a margin of 10px
make inner divs circuler in shape & give them individiul colors(red,yellow & green)